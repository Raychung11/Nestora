; Nestora — PHP upload limits (PHP-FPM / CGI hosts read .user.ini).
; Keep these in sync with the mod_php fallback in .htaccess.
upload_max_filesize = 25M
post_max_size = 30M
max_file_uploads = 20
max_execution_time = 120
max_input_time = 120
memory_limit = 256M
